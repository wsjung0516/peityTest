import { BarPeityDirective } from './bar-peity.directive';

describe('BarPeityDirective', () => {
  it('should create an instance', () => {
    const directive = new BarPeityDirective();
    expect(directive).toBeTruthy();
  });
});
