import { PeityDirective } from './peity.directive';

describe('PeityDirective', () => {
  it('should create an instance', () => {
    const directive = new PeityDirective();
    expect(directive).toBeTruthy();
  });
});
